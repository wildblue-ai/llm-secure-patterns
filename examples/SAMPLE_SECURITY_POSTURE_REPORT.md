═══════════════════════════════════════════════════════════════════
          LLM SECURITY POSTURE REPORT
═══════════════════════════════════════════════════════════════════

  Project:    /Users/cheryladay/projects/tmp/chat-demo4
  Date:       2026-08-17
  Generated:  llm-secure-patterns v0.9.0
  Framework:  OWASP Top 10 for LLM Applications 2025 (Nov 2025)

═══════════════════════════════════════════════════════════════════

  COVERAGE SUMMARY: 2 of 7 code-time-addressable categories mitigated

  | Category                                    | Status |
  |---------------------------------------------|--------|
  | LLM01: Prompt Injection                     | MITIGATED (low) — endpoint auth, input caps, and per-user budgets mitigate the access-control and abuse aspects of direct injection; no injection classifier (Level C declined), indirect injection out of scope |
  | LLM02: Sensitive Information Disclosure     | NOT ADDRESSED |
  | LLM05: Improper Output Handling             | NOT ADDRESSED |
  | LLM06: Excessive Agency                     | NOT ADDRESSED |
  | LLM07: System Prompt Leakage                | NOT ADDRESSED |
  | LLM09: Misinformation                       | NOT ADDRESSED (only partially addressable in general) |
  | LLM10: Unbounded Consumption                | MITIGATED (moderate) — token-budget rate limiting, spend circuit breaker, input/output token caps, JWT auth |

  3 categories are not addressable by code-time guidance (LLM03, LLM04,
  LLM08) — see the section at the end of this report.

  ─────────────────────────────────────────────────────────────────
  SKILL COVERAGE SUMMARY

  | Skill                  | Tier applied | OWASP IDs    | Files |
  |------------------------|--------------|--------------|-------|
  | LLM Endpoint Hardening | B (Moderate) | LLM01, LLM10 | app/main.py, app/auth.py, app/limits.py |

  Skills that did NOT fire are omitted from this table — see the
  Coverage Summary above for unaddressed categories and which skills
  would address them.

  Unattributed security comments (not placed by this plugin):
  app/auth.py:32 and app/main.py:72 carry ad-hoc "fail closed" SECURITY
  notes without a plugin signature; they are not counted toward coverage.

───────────────────────────────────────────────────────────────────
  METHODOLOGY

  This report was compiled by collecting structured # SECURITY:
  comments placed by llm-secure-patterns skills at the time each
  mitigation was implemented. Comments are mapped to the OWASP Top
  10 for LLM Applications 2025 framework, which addresses
  AI/LLM-specific security risks. This report does not address
  general application security (see OWASP Top 10 for traditional
  web application risks). Gap analysis (categories with no
  annotations) was performed by LLM analysis of the codebase and
  should be verified by a qualified reviewer.

  Automated security guidance is not a substitute for human review
  of code. This report documents mitigations applied, not security
  achieved. See SCOPE.md for full limitations.

  Generated by llm-secure-patterns v0.9.0
  For comprehensive AI security assessment: hello@wildblue.ai
═══════════════════════════════════════════════════════════════════
          DETAILED FINDINGS
═══════════════════════════════════════════════════════════════════

LLM01: Prompt Injection — MITIGATED (low)
  Verified mitigations:
    - JWT authentication on the API endpoint itself (pinned algorithm
      allowlist, `alg: none` rejected, exp/aud/iss validated, fail-closed
      when unconfigured)
      → app/auth.py:3 | Level: B (Moderate) | Confidence: [not recorded]
    - Per-user token budget and abuse limiting (raises the cost of
      repeated direct-injection probing through the endpoint)
      → app/limits.py:3 | Level: B (Moderate) | Confidence: [not recorded]
  Risk acceptance: Level C (High) declined — no per-request injection
    classifier, no per-tier credentials or priority queue. Level B
    addresses access control and cost, not injection content.
  To upgrade: re-run LLM Endpoint Hardening at Level C (adds an input
    classifier scoring requests for injection likelihood; note
    adversarial examples can still evade classifiers).
  Gaps: direct-injection content is forwarded to the model unexamined;
    indirect prompt injection (via tool output, RAG, external content)
    is out of this skill's scope — see Secure External Ingestion and
    System Prompt Design if those surfaces are added.
  Staleness: none (applied 2026-08-17, plugin v0.9.0, OWASP 2025).

LLM02: Sensitive Information Disclosure — NOT ADDRESSED — model output
  is returned to the caller verbatim (app/main.py, /chat response) with
  no PII/secret scanning; request logging deliberately omits message
  content, which limits log-side exposure. Apply Output Validation and
  System Prompt Design.

LLM05: Improper Output Handling — NOT ADDRESSED — LLM output is
  forwarded to clients as JSON at app/main.py:120 without validation or
  encoding guidance for downstream renderers. If any consumer renders
  this as HTML/markup, stores it, or passes it to another service,
  apply Output Validation.

LLM06: Excessive Agency — NOT ADDRESSED — it does not currently appear
  that your app grants the LLM tools, function-calling, or autonomous
  actions (no `tools` parameter, no MCP, no agent loop). If tools are
  added, apply Agent Action Surface Control.

LLM07: System Prompt Leakage — NOT ADDRESSED — it does not currently
  appear that your app uses a system prompt (no `system` parameter in
  the request). If one is added, apply System Prompt Design.

LLM09: Misinformation — NOT ADDRESSED (only partially addressable in
  general) — no output validation is applied, and even with Output
  Validation applied, code-time guidance does not address the
  hallucination root cause; maximum achievable status is PARTIALLY
  MITIGATED.

LLM10: Unbounded Consumption — MITIGATED (moderate)
  Verified mitigations:
    - Output-token cap (`max_tokens`) on every model call
      → app/main.py:23 | Level: B (Moderate) | Confidence: [not recorded]
    - API key sourced from environment/secrets manager, never hardcoded
      or model-visible
      → app/main.py:33 | Level: B (Moderate) | Confidence: [not recorded]
    - Service-wide spend circuit breaker with per-request ceilings
      bounding worst-case overshoot
      → app/main.py:53 | Level: B (Moderate) | Confidence: [not recorded]
    - Per-user hourly token budget with atomic check-and-reserve;
      accurate `count_tokens` input cap that fails closed; cheap
      character pre-filter preventing count-API amplification
      → app/limits.py:3, app/main.py:60-79 | Level: B (Moderate) | Confidence: [not recorded]
    - Request logging with user id + input/output token counts for
      forensic analysis
      → app/main.py:102 | Level: B (Moderate) | Confidence: [not recorded]
  Risk acceptance: Level C (High) declined — no per-tier budgets,
    cost-priority request queue, or per-tier credentials.
  To upgrade: re-run LLM Endpoint Hardening at Level C. Before running
    multiple workers/pods, back the limiter and spend monitor with
    Redis (atomic INCRBY + TTL) — the in-memory state is per-process.
  Gaps: distributed attacks across many accounts and slow-drip attacks
    staying just under thresholds remain possible; in-memory limiter
    state silently degrades to per-process limits under multi-worker
    deployments.
  Staleness: none (applied 2026-08-17, plugin v0.9.0, OWASP 2025).

═══════════════════════════════════════════════════════════════════
          NOT ADDRESSABLE BY CODE-TIME GUIDANCE
═══════════════════════════════════════════════════════════════════

  Real OWASP LLM risks, but outside what code-time patterns can
  mitigate — handle these through process and infrastructure controls.

  LLM03: Supply Chain — Requires organizational controls: model
    provenance verification, dependency auditing, signed artifacts, and
    trusted registries.
  LLM04: Data and Model Poisoning — Requires controls at the training
    and fine-tuning stages: data provenance, anomaly detection in
    training metrics, and access controls on model artifacts.
  LLM08: Vector and Embedding Weaknesses — Requires controls at the
    retrieval layer: access control on vector stores, embedding
    integrity verification, and retrieval result filtering.

  Full guidance: https://owasp.org/www-project-top-10-for-large-language-model-applications/ (published November 2025)
