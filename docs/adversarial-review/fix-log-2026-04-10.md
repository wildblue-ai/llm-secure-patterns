# Fix Log — 2026-04-10/11

Adversarial review fix session. Addressing 47 fix-now items from Run 1 (2026-04-07).
Tracking every action taken.

---

## Phase 1: Agent Work (worktree isolation)

Two agents launched in parallel with `isolation: "worktree"` to edit different file sets.

### Agent 1: SKILL.md language + effectiveness fixes (worktree-agent-a47b86d1)
Files modified:
- `skills/secure-external-ingestion/SKILL.md` — 2 edits: delimiter caveat, Level C effectiveness filled
- `skills/llm-endpoint-hardening/SKILL.md` — 7 edits: "critical defense" → "critical layer", "strong cost protection" → "cost risk reduction", "Comprehensive" → "Layered", Level C effectiveness filled, "must" → "should", added indirect injection note, added output token note
- `skills/output-validation/SKILL.md` — 3 edits: scoped "everything" claim, filled Level B effectiveness, added indirect injection note
- `skills/system-prompt-design/SKILL.md` — 7 edits: credential effectiveness reworded, anti-extraction caveat added, "significantly reduces" hedged, Level C effectiveness filled, "prevention" → "risk reduction", "protecting" → "mitigating", added indirect injection note
- `skills/agent-action-surface/SKILL.md` — 6 edits: "doesn't prevent" → "may not fully mitigate", "architecturally sound" → "defense-in-depth", "strong architectural" → "defense-in-depth approach", "strongest architecture" → "strongest layered approach", cost/latency claim corrected, "Skill 3" forward reference replaced

### Agent 2: Python template fixes (worktree-agent-af370a7d)
Files modified:
- `templates/python/sanitize_web_content.py` — 7 fixes: expanded ZERO_WIDTH_CHARS (+6 chars), added Unicode Tags block stripping (U+E0000–U+E007F), updated remove_zero_width_chars docstring, fixed delimiter injection in wrap_as_untrusted, fixed ReDoS in fallback regex (length-limited), fixed "prevent" → "eliminate" in demo, tightened ROT13 common_words
- `templates/python/output_schema_validator.py` — 3 fixes: reordered scan-before-sanitize pipeline + fail-closed on critical warnings, expanded URL pattern (javascript:/data:/blob:/ftp: + protocol-relative), added low-fidelity caveat to scan_for_prompt_leakage
- `templates/python/system_prompt_template.py` — 3 fixes: delimiter injection (key sanitization + content escaping), "prevent" → "eliminate" language, credential detection caveat
- `templates/python/isolated_pipeline.py` — 5 fixes: delimiter injection in wrap_cross_model_output, delimiter injection in wrap_tool_result, startswith() instead of substring for DESTRUCTIVE_TOOL_PREFIXES, unknown trusts_input_from validation, advisory-only warning in validate_pipeline docstring
- `templates/python/token_budget_limiter.py` — 3 fixes: TOCTOU/multi-process/memory warnings, estimate_tokens non-Latin warning, max_tokens guidance comment

### Worktree incident
Agents did NOT commit their changes to the worktree branches. On attempted merge, branches showed "Already up to date." However, the edits persisted in the main working directory (the worktree writes went to the same filesystem). All agent edits were verified present via grep/diff. See `docs/TROUBLESHOOTING.md` for root cause and prevention.

---

## Phase 2: Manual verification and gap-filling

After confirming agent edits persisted, manually checked each finding against the code and filled remaining gaps:

### Sweep 1: Language violations — VERIFIED COMPLETE
All "prevent/protects/comprehensive/critical defense" instances replaced. Confirmed via grep.

### Sweep 2: Effectiveness fields — COMPLETED MANUALLY
Agent filled: SEI Level C, EH Level C, OV Level B, SPD Level A/C, AAS (had none blank).
Manually filled: SEI Level A, SEI Level B, EH Level A, EH Level B, SPD Level B.

### Sweep 3: Delimiter injection — VERIFIED COMPLETE
All 5 wrapping functions now escape closing delimiters. Key sanitization added to build_system_prompt.

### Sweep 4: Indirect injection cross-references — COMPLETED MANUALLY
Agent added: llm-endpoint-hardening, output-validation, system-prompt-design.
Manually added: agent-action-surface (note on indirect injection via tool results).
secure-external-ingestion: not needed (it IS the indirect injection skill; added content-type note instead).

### Sweep 5: Unicode tag-character coverage — VERIFIED COMPLETE
U+E0000–U+E007F Tags block, U+2061–2064, U+FFA0, U+3164 all added. "etc." removed from docstring.

### Skill-specific: secure-external-ingestion — VERIFIED COMPLETE
ReDoS fix, ROT13 tightening, delimiter escaping, scope note, content-type note all confirmed.

### Skill-specific: llm-endpoint-hardening — VERIFIED COMPLETE
TOCTOU warning, token estimation warning, multi-process warning, memory caveat, max_tokens note, scope reframe all confirmed.

### Skill-specific: output-validation — COMPLETED MANUALLY
Agent fixes confirmed. Additionally fixed: TypeScript Level B Zod example missing try/catch (line 122), also fixed scan-before-sanitize order in TypeScript example to match Python fix.

### Skill-specific: system-prompt-design — VERIFIED COMPLETE
Anti-extraction caveat, key sanitization, content escaping, language fixes all confirmed.

### Skill-specific: agent-action-surface — VERIFIED COMPLETE
startswith fix, trusts_input_from validation, validate_pipeline caveat, delimiter injection, cost claim, defense-in-depth language all confirmed. Added indirect injection note.

### SCOPE.md — COMPLETED MANUALLY
Added "Known Limitations Identified by Adversarial Review" section with 7 won't-fix categories:
HTTP/network-layer attacks, image-based/OCR injection, ASCII-art/leetspeak bypasses, polyglot/multi-encoding chains, timing side-channels, cross-agent CSRF/replay, multi-turn priming.

---

## Phase 3: Inline caveats and snippet warnings (2026-04-06, continued session)

Added targeted inline caveats to SKILL.md files to surface known limitations directly at the point of use:

- `skills/llm-endpoint-hardening/SKILL.md` — Level A: `# WARNING: len() counts characters not tokens` in Python snippet; Level B: `# WARNING: estimate_tokens uses len/4 heuristic` and TOCTOU `# WARNING: check_budget + record_usage is NOT atomic` in Python snippet
- `skills/output-validation/SKILL.md` — Level B: `html.escape()` context caveat (safe for text-node only, NOT attribute/JS/CSS/URL); prompt leakage caveat (detects cooperative/accidental disclosure only, silent injection passes clean); `validate_llm_output` snippet caveat (simplified — callers must check warnings; full fail-closed logic in template)
- `skills/secure-external-ingestion/SKILL.md` — Level B: encoding normalization trade-off caveat (decoder actively reveals encoded payloads into readable plaintext; decoded text must still be treated as untrusted)

---

## Files modified (total: 12)

| File | Changes |
|---|---|
| `skills/secure-external-ingestion/SKILL.md` | effectiveness fields, content-type note, delimiter caveat |
| `skills/llm-endpoint-hardening/SKILL.md` | language fixes, effectiveness fields, indirect injection + output token notes |
| `skills/output-validation/SKILL.md` | scoped claim, effectiveness, indirect injection note, TypeScript try/catch |
| `skills/system-prompt-design/SKILL.md` | language fixes, anti-extraction caveat, effectiveness, indirect injection note |
| `skills/agent-action-surface/SKILL.md` | language fixes, cost claim, defense-in-depth, indirect injection note |
| `templates/python/sanitize_web_content.py` | Unicode expansion, delimiter escape, ReDoS fix, ROT13 tightening, language |
| `templates/python/output_schema_validator.py` | pipeline order, fail-closed, URL expansion, prompt leakage caveat |
| `templates/python/system_prompt_template.py` | delimiter injection, key sanitization, language, credential caveat |
| `templates/python/isolated_pipeline.py` | delimiter injection x2, startswith fix, trusts_input_from validation, advisory caveat |
| `templates/python/token_budget_limiter.py` | TOCTOU/multi-process/memory warnings, token estimation, max_tokens note |
| `SCOPE.md` | won't-fix section with 7 adversarial review categories |
| `docs/TROUBLESHOOTING.md` | new file: worktree, Gemini API, SSH troubleshooting |
